# [](https://)[](https://)CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

`customer-support-agents` project aims to demonstrate how human-in-the-loop can be combined with AI workflow automation to improve business efficiency and efficacy.  

## Development process

When instructed to build a feature:

1. Use your Atlassian tools to read the feature instructions from Jira
2. Develop the feature - do not skip any step from the feature-dev 7 step process
3. Always use `uv sync` with `pyproject.toml` instead of `pip install` with `requirements.txt` for backend build
4. Thoroughly test the feature with unit tests and integration tests and fix any issues
5. Submit a PR using your github tools

## Commands

### Backend (run from `backend/`)

```bash
USE_MOCK_LLM=true uv run pytest -v   # run all backend tests
uv run uvicorn main:app --reload     # dev server on :8000
```

### Frontend (run from `frontend/`)

```bash
npm run dev          # dev server on :3000 (hot reload, proxies /api to :8000)
npm run build        # static export to frontend/out/
npm test             # vitest run (single pass)
npm run test:watch   # vitest watch mode
# npm run lint         # eslint — not yet configured (next lint prompts interactively; no eslint config committed)
```

### Docker (run from repo root)

```bash
scripts/start-mac.sh   # docker compose up --build -d
scripts/stop-mac.sh    # docker compose down
```

## AI design

Part of codes are originated from [langgraph-interrupt-workflow-template](https://github.com/KirtiJha/langgraph-interrupt-workflow-template) which is designed calling AI models agostically (tag along with LangChain's `init_chat_model`) and also pluged in with mocked LLM implementation in case LLM calling failed. 
There is an OPENAI_API_KEY in the .env file in the project root.

## Technical design

The entire project should be packaged into a Docker container.

The backend is in `backend/` — a `uv` project (Python 3.12) using FastAPI.  Currently, all API endpoints are in in `backend/main.py`. I have already separated into `route` and `services` and `models`. `models/model.py` have all model schemas shared by `auth` and `customer-support` services.  

The frontend is in `frontend/` — Next.js 16, React 19, Tailwind 4. It will be statically exported (`next.config.ts` → `output: "export"`) to `frontend/out/`, which FastAPI serves at runtime. 

I decide to use DEMO_MODE for prototype because return refund involves the product return window. The data in database will be stale and not testable.  I can adjust data in memory in DEMO mode to always be testable and I have done that.

Backend is available at http://localhost:8000

## Architecture

### Request flow summary

The main workflow will create customer-support AI agents that help general FAQs, order inquiry and more importantly, automate refund request process.  However, human-in-the-loop approve/ reject/ flag workflow will get involved when requests exceeds certain thresholds: ex. refund amount > 500 (configurable) or disputable non-refundable return/ refund request (escalated to human approval). 

I plan to use Slack Bot interactivity to implement the approve/ reject/ flag workflow for return/ refund requests. I have already got n8n app (AI workflow automation) to integrate successfully with Slack Bot interactivity for the same workflow.  See [Slack Bot Interactivity Screenshot for the the approve/ reject/ flag workflow of a refund request request ](n8n-slack-interactivity/slack-screenshot-screenshot-refund-request-approval.png) and have configured `customer-support-dev` app mimicing `n8n-integration`'s and will code the main workflow soon.  

Workflows are orchestrated with LangGraph. The main workflow will be like: a customer login in with its account email address using `api/auth`. API will lookup customer orders if the customer is a valid `customer` role. Despite my best effort, I cannot prevent customers from asking question of general policy. To manage the flow easier, I add a Customer initial selections screen with 'Order inquiry & return refund' button, 'Others' button and 'Exit' button. We will display the full list of FAQs described in workflow/customer_support_tools.py of 3 sections: Shipping & Delivery, Returns and Refunds and Orders and Payment in an highlighted formal way.  Hopefully, customers will read and find their answers from FAQs and do not prceed further. Customers' general questions would be handled either by fuzzy match or LLM match if either of them would be helpful based upon LLM's reasoning and judgement. It will be escalated to human CSR (send to a Slack channel eventually) if the customer-support agent cannot find an answer meeting the confidence gate.

For 'Order inquiry & return refund', in initial screen, we will display the summary of customer's latest orders in descending order from `CustomerContext` with check boxes, the customer can select the order he/ she is concerned about. It will hit (`api/support/order`) endpoint. The backend can retrieve the target order and the UI can display the order details (status, delivered_date, estimated_delivered_date, ship_date, tracking_number, order notes) so that LLM can strike meaningful conversation with the customer. This might be the most difficult part.  Despite throrough system prompt/ instructions, There is no script (system prompts) can cover all the scenario. customer-support-agent is instructed that he/ she should escalate to human CSR if csa don't know the answer to avoid 'hallucination'. LLM have structured output:
response, `escalate_is_required` and `intent_for_return_refund`

If the customer request return/ refund and does not accept exchange option . we need to check return refund eligible. We cannot check until the customer select items and quantity to return. 
UI will display itemized order with check boxes and quantity filled in and the customer can override quanties and check boxes etc when `customer_intent_to_return` flag is one.  The result of itemized order with check boxes and quantity will be sent to (`api/support/return/start`) 

The return/ refund workflow here will be 
1. create a return/ refund request, return order and return order item(s), 
2. calculate total refund amount including tax
3. figure out if return refund is eligible: orders exceeding the return window or try to return used or perishable items are non-refundable.

Only auto refundable orders and human refundable orders are allowed to proceed and added to the return refund queue. For auto refundable orders, CSA will mark refund status `auto_approve` and send the confirmation letter (configurable) along with shipping labels if provided.  Refund requests requiring human approval will go to human_review node.  We will issue LangGraph `interrupt` statment and LangGraph will suspend the operation of current thread.  UI will tell the user: Customer support representative will review the refund request in Slack and will respond shortly.  The message should be in the response as usually.

HITL `interrupt` will trigger sending customized Slack Interactivity message to Slack and also schedule a TTL (configured to 5 minutes) task and cache the task (key: thread_id, value: task) and  When huamn CSR press Approve or Reject or Flag button in Slack with optional reason, slack will post the response info along with `thread_id` and `refund_request_id` back as designed to our API webhook (The webhook in local environment will be exposed as a public URL via `ngrok` reverse proxy) which will 
1. send ackowledgement to Slack
2. update the refund request with Slack interactivity response
3. call a fuction TBD to verify with thread-task cache if the TTL has been up, if not, cancel the task, clear the cache and calling `api/support/return/decide` 

The TTL scheduled task will call `api/support/return/decide` if it is up.

`api/support/return/decide` will issue Command(resume=xxx).  LangGraph will resume at the checkpoint following the `interrupt`.  The flow
will check the refund request status to update the final result (HITL Slack interactivity response come in on time or TTL timeout on HITL and the response is not coming yet) of the state.

The above screen is for customers. There should be a separate screen for customer-support team.  From visibility perspective, `customer-support-agents`  is a small `multi-tenant` app.  It's recommended to use domain names(s) to configure customer-support team in production environment. 

For `customer-support` team,  they cannot see order/ return inquiry screen.  However, they can see return/ refund requests report screen.  The initial phase will include  `Ask AI to draft customized customer-support letter` task. That will reference GET api/approval (codes are in `frontend/app/approval/page.tsx`).  Once the query/ task is filled in and submited, it will trigger POST  `api/approval/start`. Then, it will trigger `api/approval/decide` once the initiator choose approve/ edit/ request change with a reason. This means to demonstrate human approval gate.  Later, we will include return/ refund inquiry/ report by date range and status (auto-approval, manual approval, reject etc.).  


### How human-in-the-loop work in LangGraph's interrupt - Command resume framework
When human-in-the-loop step is needed, issue `interrupt` with key-value dictionary of info to convey to the frontend like actions (action buttons), and message. LangGraph captures state in StateSnapshot including all accumulated messages (HumanMessage, AIMessage and ToolMessage).  Each message comes with argument details and checkpoint_id. In LangGraph, you can invoke a config with thread_id and checkpoint_id, it will execute/ resume the worklfow of that thread at that checkpoint_id. That's how you can test partial workflow in LangGraph. `interrupt` will release the thread.  When a human press a button, API will issue a Command(resume=...). Langugraph will resume the workflow at the point following `interrupt`.  It's important that `interrupt` need to be at the very beginning of that node.  Any preparation should be in a separate node.

Why do we need interrupt - Command resume mechanism for HITL? 
- It's a dynamic workflow: `interrupt` can be triggered or not depending upon thresholds or circumstance.  We might escalate due to customers' frustration on edge case.
- The normal flow is UI -> API -> LangGraphWorkflow.  Need a way to signal HITL is required.
  
Why if a customer-support member has never pressed a button? What would be the impact on UI?
- LanGrgaph does not hold on that thread.  However, the thread is in interrupted/ paused state. UI screen will be frozen (not responsive) until the Command(resume=...) is issued.  A config with thread_id in upted/ paused state cannot be re-invoke.
- When we insert a return/ refund request business object/ entry, we will also insert a interrupt management object/ entry.  There will be an interruption TTL. When a customer-support member press a button before the TTL expire, it will update both request business object as well as interrupt state mgmt object, and issue Command(resume...).  If TTL expire prior to any human inteervention, it will update interrupt state mgmt object, and issue Command(resume...). From user perspective, the request was timeout (customer-support member might be late to repond). It won't touch the business object.   When a customer-support member finally respond, it will check the state of interrupt state mgmt object and not to re-issue Command(resume...).  However, it will still update the request business object and send the confirmation email ex.  The interrupt management entries are temporary records and will be cleaned up by DB TTL.


### Key files

### Routing guard

The single-container image serves both the API and the Next.js static export (`frontend/out`) from one FastAPI app (`backend/main.py`):

- All API routes are registered under `/api/*` and are added to the router before the catch-all, so they always match first.
- `/_next` is mounted via `StaticFiles` (guarded by an `is_dir()` check, since `frontend/out` won't exist when running the backend standalone in dev).
- A catch-all `GET /{full_path:path}`, registered last, serves the rest of `frontend/out`: it 404s immediately if `full_path` starts with `api/` (explicit guard, on top of registration order already protecting this), then tries the exact file, `{full_path}.html`, `{full_path}/index.html`, and — only for the empty path (`/`) — `index.html`. Any other unmatched path falls through to the export's own `404.html` (status 404), so typos/dead links don't silently render the homepage with a 200.
- Path traversal is blocked by resolving each candidate and checking `candidate.is_relative_to(FRONTEND_DIR)` before serving it.

### Sqlite persistence paths

`CHECKPOINT_DB` and `STORE_DB` (used in demo mode for durable `AsyncSqliteSaver`/`AsyncSqliteStore`) are configured as relative filenames, but `sqlite3`/`aiosqlite` resolve relative paths against the process's **current working directory at connect time** — not against `.env`'s or `main.py`'s location. Launching `uvicorn`/`pytest` from `backend/` vs. the repo root used to produce sqlite files in different places for the exact same `.env` (and `STORE_DB` needed a `../data/` prefix hack just to land next to `CHECKPOINT_DB` at all).

Fixed by anchoring both at `DATA_DIR = BACKEND_DIR.parent / "data"` in `main.py` (`BACKEND_DIR = Path(__file__).resolve().parent`), via a `_resolve_db_path()` helper:
- A relative filename (e.g. `checkpoints.sqlite`) always resolves under `DATA_DIR` — the repo root's `data/` locally, `/app/data` in the container (Docker's `WORKDIR` puts `main.py` at `/app/backend`, so `BACKEND_DIR.parent` is `/app`), matching the `./data:/app/data` volume mount in `docker-compose.yml`.
- An absolute path is used as-is (an operator override).

`.env`/`.env.example` now just use bare filenames (`checkpoints.sqlite`, `stores.sqlite`); leaving both unset still falls back to in-memory `MemorySaver`/`InMemoryStore` as before.

### Summarize and store in long-terms memory
I did some research on wheather we should summarize messages, store in the long-term and prune messages. Yes, whatever I implement in `summarize_node` (originated from https://pub.towardsai.net/your-first-real-langgraph-project-ac5eb00f923a) is what LangGraph recommends. In `summarize_node`, RemoveMessag mean to delete `messages` in `CustomerSupportState` not `messages` in `StateSnapshot`.  However, judging from `test_general_support_thread_accumulates_and_summarizes` unit test, it also prune `messages` in `StateSnapshot`. What is going on?  Yes, CustomerSupportState store all variables, including `messages` under `values` in `StateSnapshot`. It's the same space. Pruning `messages` in `CustomerSupportState` is the same as removing `messages` under `values` in `StateSnapshot`.  That's core knowledge of LangGraph/ LangChain just as knowing how to resume the state of a thread.  

## Color Scheme

- Accent Yellow: `#ecad0a`
- Blue Primary: `#209dd7`
- Purple Secondary: `#753991` (submit buttons)
- Dark Navy: `#032147` (headings)
- Gray Text: `#888888`

## Implementation Status

### Completed
- CSA-2: Moved from two Docker containers (separate frontend + backend) to one. `Dockerfile` is a multi-stage build — stage 1 (`node:20-alpine`) runs `npm run build` against `frontend/` to produce the static export at `frontend/out` (`next.config.ts` sets `output: "export"`); stage 2 (`python:3.12-slim`) installs the backend with `uv sync --no-dev` and copies `frontend/out` in alongside it. `docker-compose.yml` runs a single service on port 8000. The root cause of the original failure (`/approval` 404ing at `:8000`) was that FastAPI never mounted/served `frontend/out` even though the image already contained it — fixed via the static-file serving + catch-all described in [Routing guard](#routing-guard). Also added: a root `.dockerignore` (host `node_modules`/`.venv`/`backend/checkpoints.sqlite` were previously copyable into the build context and image), and the frontend's `API_URL` now defaults to the relative `/api` instead of an absolute `http://localhost:8000/api`, so the same build works regardless of the host/domain the single container is served from.

- CSA-3: [API] Re-organize, build the infrastrure: cross-cutting long-term memory and clean-up
  - Move approval API calls out of main.py. Re-organize to have separate route/service and shared models. The approval API functions are in `routes/approval.py` now.  Two new routes/ services: auth and customer-support would follow the new structures.  `models/model.py` , having core schemas: CustomerSupportState, CustomerContext, Order, OrderItem, RefundRequest, ReturnedOrder, ReturnedOrderItem etc., are shared by auth and customer-support routes/ services
  - I make use of LangGraph cross-cutting long-term memory.  Add long-term memory (so called store) wiring in the `lifespan` function of main.py. `customer-support` workflow is using long-term memory.
  - Update `save_user_memory` in memory.py to use thread_id as the key.  There will be one summary for a conversation thread. 
  - `load_user_memory` will look up by user_id (customer_id).  It will be called before LLM start conversations with the user in `customer-support` screens so that LLM have the context of previous conversations
  - Add `summarize_node` to customer-support workflow to summarize if the conversation exceeds 6 messages or we just cut-off a series of conversations of one topic
  - I retire `agent` set. I use plain `interrupt` and `Commmand(resume)` instead of HumanInTheLoopMiddlewar. 
  
- CSA-4: Add `auth` route/service
    - Use `DEMO_MODE` only in prototype (demo_data is in memory).  I states the reason in `Request flow summary`
    - I apply `adjust_days_demo_data_testable` logic to `demo_data` in memory by test customer's assigned `OrderRefundStatus` so that they are always testable.
    - `customer-support-agents` is a multi-tenant application.  `auth` service returns `AuthResponse` with a role of (`csr` or `customer`) by the login user email. `csr` is regulated by `CSR_WHITELIST` and `CSR_DOMAINS` defined.  The former has higher priority. Then fall back to search customer order system for `customer` role. It will return is_auth = False if it cannot find the customer. `AuthResponse` is prefilled with `CustomerContext` of recent orders for users of `customer` role. 

- Post CSA-3/CSA-4 bug-fix pass (PR #4): general_inquiry flow was left half-done after CSA-3/CSA-4, so `order_inquiry_node`/order-return flow is still a stub (CSA-8 will build it out) — these are the bugs fixed on the general_inquiry side plus a couple that predate it:
  - `services/auth_service.py`: fixed `elif`/`else` short-circuiting the CSR whitelist + domain checks (the domain check was skipped whenever a whitelist was configured, even if the email didn't match it); return `is_auth = False` instead of raising 401/404 when a customer can't be found or fails `CustomerContext` validation, so `/api/auth` always returns a consistent `AuthResponse` shape
  - `routes/customer_support.py`: fixed a `str.join()` crash on every `/api/support/start` call (was passing two positional args instead of a list)
  - `workflow/customer_support.py`: fixed `general_issue_resolved` never being set on the non-escalate branches of `general_faq_eval_node`/`general_faq_fuzz_match_node`, which raised `KeyError` in the routers on the normal/resolved path — declaring a field in the `ChatState` TypedDict does **not** give it a runtime default; LangGraph only exposes a channel once something actually writes it. Also reworked `summarize_node` routing: it's now reachable from every terminal branch of the general_inquiry flow (not just the LLM-match path) via a shared, flow-agnostic `_should_summarize(state, topic_concluded)` helper — each flow passes its own "topic concluded" signal (`general_issue_resolved` for general_inquiry), and the message-count threshold (`SUMMARIZE_MESSAGE_THRESHOLD = 6`) is the only generic part, kept out of `summarize_node` itself so future flows (order/return, CSA-8) can reuse it without depending on a field that's specific to general_inquiry
  - `memory.py`: `_namespace()` passed `customer_id` (an `int`) straight into the store namespace tuple; LangGraph stores require string namespace segments, so `save_user_memory`/`load_user_memory` were silently failing (swallowed by their own `try/except`, logged only as a `WARNING`) on every single call — cross-session memory never actually persisted until this was fixed
  - See [Sqlite persistence paths](#sqlite-persistence-paths) for the `CHECKPOINT_DB`/`STORE_DB` path-resolution fix

- CSA-5/CSA-6/CSA-7: [UI] Login screen, CSR draft-approval screen, customer initial-selections screen (built together on one branch/PR since CSA-5's redirects land directly on CSA-6/CSA-7)
  - CSA-5: new `frontend/app/page.tsx` (root route) — single email-address input, calls `POST /api/auth`, shows the required "unable to find any customer" message when `is_auth` is false (keying off `is_auth`, not `role`, since the backend defaults `role` to `"customer"` even on failure), and redirects to `/customer` or `/csr` by `role` on success. No logo exists, so the login card shows a handful of FAQ questions (first two per section from `lib/faq.ts`) as decorative, non-interactive tags.
  - CSA-6: `frontend/app/approval/page.tsx` (the only page that existed before this work) was `git mv`'d to `frontend/app/csr/page.tsx` and wrapped with a `useRequireRole("csr")` guard; its draft/approve/edit/reject state machine against `/api/approval/start`/`/api/approval/decide` is otherwise unchanged. The "return/refund report screen" mentioned in the ticket is not part of this work — only the already-built draft-letter approve/edit/reject gate was ported, per the ticket's own "the latter part is ready" framing.
  - CSA-7: new `frontend/app/customer/page.tsx` — greets the customer from `session.customer_context`, displays the full 3-section FAQ list from `lib/faq.ts` as a collapsible accordion, and offers three actions: "Order inquiry & return refund" and "Others" navigate to placeholder pages (`frontend/app/customer/order-inquiry/`, `frontend/app/customer/others/`) since their real destinations belong to un-started tickets (no order-inquiry UI ticket yet; "Others" is CSA-9, backed by CSA-8); "Exit" clears the session and returns to `/`.
  - New `frontend/lib/auth-context.tsx`: a React Context (`AuthProvider`, `useAuth`, `useRequireRole`) persisting `{email_addr, role, customer_context}` to `localStorage`, hydrating on mount so a page refresh doesn't lose the session. `useRequireRole(role)` redirects unauthenticated or wrong-role visitors to `/` and returns `session: null` whenever unauthorized (including on a role mismatch) so guarded pages only need `if (isLoading || !session) return null;` — no per-page re-check of `session.role`.
  - New `frontend/lib/faq.ts`: a **generated** file (do not hand-edit) produced by `backend/scripts/export_faq.py` from `workflow/customer_support_tools.py`'s `FAQs` dict, keeping the frontend's FAQ content and the backend's single source of truth from drifting apart. `backend/tests/test_faq_export_freshness.py` fails the backend test suite if the committed file is stale — regenerate with `uv run python -m scripts.export_faq` (from `backend/`) after editing `FAQs`.
  - Brand colors (Accent Yellow/Blue Primary/Purple Secondary/Dark Navy/Gray Text, see [Color Scheme](#color-scheme)) are now wired into `frontend/tailwind.config.js` as `brand.*` tokens and used across the login, CSR, and customer screens — this is the first UI work to actually apply the documented palette.
  - Bootstrapped frontend testing (previously nonexistent): `vitest` + `@testing-library/react` + `jsdom`, with `frontend/vitest.config.ts`/`vitest.setup.ts` and a shared `frontend/lib/test-utils.tsx` (`renderWithAuth`, `jsonResponse`). Coverage: `frontend/app/page.test.tsx`, `frontend/app/csr/page.test.tsx`, `frontend/app/customer/page.test.tsx`.
  - Root `.gitignore` had a blanket `lib/`/`lib64/` Python-venv rule that was unintentionally shadowing the new `frontend/lib/` directory — removed, since `.venv/`/`venv/`/`env/` already cover virtualenv `lib/` dirs.

- CSA-8: [API & workflow] customer-support generic API and general/ other workflow — scoped to the `general/ others` FAQ path only; `order_inquiry_node`/return-refund flow stays a stub for a later ticket.
  - `routes/customer_support.py` collapsed down to a single `POST /api/support/general` endpoint (dropping the earlier `/api/support/start` + `/api/support/continue` pair), delegating to a new `services/customer_support_service.py::invoke_general_support_workflow`. Users are allowed to switch support category, so there's no need to special-case the first turn from the rest — one endpoint handles both.
  - Implemented the `general_faq_eval_node` → `general_faq_fuzz_match_node` → `general_faq_llm_match_node` chain per the ticket's design: `general_faq_eval_node` asks the LLM (`FAQMatchEvals` structured output) whether a rapidfuzz match, an LLM semantic match, neither, or both would help; the fuzz node runs `find_closest_faq` (rapidfuzz `partial_ratio`, not `WRatio` — the scorer was switched mid-ticket and the field/docs/system-prompt renamed to match: `rapid_fuzz_partial_ratio_match_helpful`) and answers directly above `FAQ_MATCH_THRESHOLD` (75), otherwise falls through to the LLM match node (`FAQMatchResult` structured output) using the same threshold; anything below threshold, or both flags False upfront, returns `ESCALATE_MESSAGE`.
  - Fixed the crash blocking the whole flow: `general_faq_fuzz_match_node` instantiated `BaseMessage` directly, which pydantic rejects (`type` field required — only concrete subclasses like `AIMessage`/`SystemMessage` supply it, and setting `type=` manually doesn't help since downstream LangChain code dispatches on the actual Python class, not that string). Switched to `AIMessage(content=response)`, matching `general_faq_llm_match_node`'s existing pattern for the same "surface the matched answer to the customer" case. Both matched-answer `AIMessage`s now also carry a `name=` source tag (`FAQ_FUZZY_MATCH_SOURCE` / `FAQ_LLM_MATCH_SOURCE`) so it's later inspectable which path answered.
  - Added multi-round support: `GeneralSupportRequest`/`GeneralSupportResponse` now carry an optional `thread_id` (defaulting to `None` so a first-ever call can omit it — Pydantic v2 requires an explicit `= None` for a field to be omittable, not just `| None` typing) that the service reuses as the LangGraph `configurable.thread_id` instead of minting a fresh UUID every call, so a second question from the same customer resumes the same checkpointed thread. Verified end-to-end (real two-round run against the compiled graph, inspecting checkpointed state) that `add_messages` correctly accumulates/prunes history and that `general_inquiry`/`faq_match_evals`/`general_issue_resolved` correctly reset each call so a new question gets a fresh evaluation. `general_faq_eval_node`'s prompt references `state["summary"]` (the running digest `summarize_node` already maintains) for prior-turn context, rather than a fixed message-list index (`messages[-2]`), which would `IndexError` on a brand-new thread's very first message.
  - `general_faq_eval_node`/`general_faq_llm_match_node` still only ever see the *current* question plus that rolling summary — not the full raw message history — when asking the LLM to judge a match; genuinely open-ended multi-turn reasoning (e.g. "no, I meant the other order") is a known gap left for a future pass.
  - `backend/tests/test_customer_support.py` added: direct node-level tests for `general_faq_fuzz_match_node` (fake `FAQMatchEvals` state, bypassing the LLM eval call entirely) plus one end-to-end `/api/support/general` test. Note: the built-in `MockChatModel` can't fake `with_structured_output` (it only knows how to fake `bind_tools`-style tool calls keyed by a `.name` attribute, which plain Pydantic schema classes don't have), so `general_faq_eval_node`/`general_faq_llm_match_node` always get `None` back under mock and always escalate — genuine "confident LLM match" scenarios for those two nodes can only be tested by faking the node's input state directly, never by driving them through `.ainvoke()` under mock.
  - `test_general_support_thread_accumulates_and_summarizes` verifies multi-round continuity against the actual LangGraph checkpoint, not just the HTTP response: `client.app.state.support_graph` (the `TestClient`'s underlying `FastAPI` app instance exposes the same compiled graph the routes use) + `await graph.aget_state({"configurable": {"thread_id": ...}})` reads back `state.values["messages"]`/`state.values["summary"]` directly. This is the only way to actually prove `RemoveMessage` pruned the stale prior-round messages rather than the list just growing unbounded — the HTTP response alone (or the frontend's own local chat transcript) can't distinguish "server-side history is correctly pruned" from "server-side history is silently piling up," since either way the customer just sees their own accumulating exchange on screen.

- CSA-9: [UI] general/ other (FAQ) starting screen — the two open UX questions the ticket itself posed were resolved before building: the submit button toggles wording ("Request an answer" → "Ask another question?" once a response comes back, per the ticket's own suggestion) rather than staying static, and "Go to order inquiry"/"Exit" stay visible at all times rather than appearing only after an answer.
  - Replaces the placeholder at `frontend/app/customer/others/page.tsx` with the real screen: greets the customer by name, takes a question, calls `POST /api/support/general` (passing `thread_id: null` on the first call so it's omittable per CSA-8's Pydantic v2 fix), and renders the growing Q&A exchange as chat bubbles. Reuses the `thread_id` from each response on the next call, so follow-up questions resume the same conversation rather than starting a new thread every time.
  - `frontend/app/customer/others/page.test.tsx` added, mirroring `csr/page.test.tsx`'s `vi.stubGlobal("fetch", ...)` pattern: covers the redirect guard, the greeting, a two-round exchange (asserting the second request's body carries the first response's `thread_id`), the order-inquiry nav, and Exit.
  - Verified beyond the unit tests: driven through an actual browser (`dev` servers + `mcp__claude-in-chrome__*`) against the real backend, not just mocks — confirmed the button-label toggle, the transcript rendering, and thread continuity via the backend log's `Deserializing unregistered type ... from checkpoint` warning appearing only on the second call (the signature of `AsyncSqliteSaver` loading a previously-persisted checkpoint). Two things worth remembering if repeating this: (1) programmatically setting a React-controlled `<input>`'s value (e.g. a browser-automation tool's `form_input`) can update the DOM without firing React's `onChange`, leaving `disabled={!value.trim()}` buttons stuck disabled — driving real keystrokes (a `type` action after a `left_click` to focus) works, seeding `localStorage`'s session key directly and navigating past the login page entirely is the more reliable route when the page under test doesn't need to exercise login itself; (2) `next dev`'s default port silently shifts (e.g. to 3001) if something else already holds 3000 — check the dev server's own log line rather than assuming the usual port.
- CSA-10: [API & workflow] This adds two endpoints: `/api/support/order/init` to serve/ respond to the `order_inquery` initial summary and order selection screen and `api/support/order/continue` to serve all the following order detail and conversation screens. 
  - `/api/support/order/init`: `order_support_init` in routes/customer_support.py, `invoke_order_init_workflow` in services/customer_support_service.py and `order_init_chat_node` in workflow/customer_support.py work together to initialize and reset `order_inquery` state, retrieve the target order and start a deterministic conversation based upon order status and other details. Yes, LLM is not involved. 
  - `/api/support/order/continue`: `order_support_continue` in routes/customer_support.py, `invoke_order_continue_workflow` in services/customer_support_service.py and `order_continue_chat_node` in workflow/customer_support.py work together to resume the state started by `/api/support/order/init` continue conversations between the customer and LLM instructed by comprehensive system prompt.
  - LLM needs to know the `summary` and `long-term memory` context to engage the conversation. However, it is efficient to inject long-term memory in `/api/support/order/continue` for each conversation.  I ends up injecting `long-term memory` in `invoke_order_init_workflow` and `summarize` at the end of `init` so that `order_continue_chat_node` can use either `summary` or `long-term memory`.  To clarify `summary` only include conversation in the current thread and does not include the previous conversation.
  - I added as many unit tests as I can. Mock LLM is the source of deficiency now.  It returns None in `order_continue_chat_node`.  Therefore I cannot finish end-to-end unit tests.  I need to resort to Swagger `localhost:8000/docs` to finish end-to-end.
  - I tried to be comprehensive to cover all scenarios in the `SYSTEM_PROMPT` of `order_continue_chat_node`.  For orders of `delivered` status, cover `shipment not found` and `return and refund` scenario, for orders of `pending`, cover backorder and `cancel order` scenario. Tracking orders in `transit` state.  I tried Swagger for end-to-end.  LLM seems to stick to the script (SYSTEM_PROMPT) and sometimes repeat the same response again and again even though SYSTEM_PROMPT SAYS `DO NOT REPEAT the same response` (I am increasing the temperature to retry it).  Also it does not know when it's the time to mark `order_issue_resolved` so that I can `summarize`.  I can use some `prompt engineering` here.
  - to continue our effort to `summarize` conversation of the thread, I add `api/support/exit` which calls `invoke_summarize_on_exit` and will set `summarize_on_exit` to True to trigger `summarize`
  - I give some thought on Messages. An `AIMessage` in LangChain is a standard object that represents the output or response generated by an AI chat model.  Therefore, the output fuzzy match function call should be a AIMessage.  I replace it with flexible and neutral `ChatMessage` and use `function` as the role for any message generated from outcome of function calls. I am using `ChatMessage` for all ESCALATE_MESSAGE or SYSTEM_ERROR_MESSAGE with the role `assistant` for messages decided by agent's codes. I elaborate more sources and standardize Messages.

  - Post CSA-10 code-review fixes: nine issues found in review, all confirmed and fixed
    - `routes/customer_support.py` / `services/customer_support_service.py`: `/api/support/exit` and `invoke_summarize_on_exit`'s `graph.ainvoke(...)` were both missing `await`, so the coroutines were created but never scheduled — the Exit button's summarize-on-exit save silently did nothing. Both now `await`ed.
    - `workflow/customer_support.py::order_continue_chat_node`: dereferenced `order_output.escalate` without checking for `None` first (every other structured-output node in this file guards this, e.g. `general_faq_eval_node`'s `if not eval or ...`). `with_structured_output(...).ainvoke(...)` returning `None` — which the mock LLM always does for this node, and which the real LLM can do on a refusal/malformed response — crashed `/api/support/order/continue` with a 500. Now falls back to `ESCALATE_REASON.HELP_ANSWER_ORDER_INQUIRY` when `order_output` is `None`.
    - `models/model.py::OrderInitResponse.target_order` was a required (non-Optional) `Order` field, but `invoke_order_init_workflow` passes `None` whenever the order lookup fails (the node already builds a graceful `SYSTEM_ERROR_MESSAGE` response for this case) — Pydantic validation crashed the response construction instead. Now `Order | None = None`.
    - `services/customer_support_service.py::invoke_order_continue_workflow`: had no guard against a `thread_id` that was never initialized via `/order/init` (or whose init failed to find an order) — `route_branch`/`order_continue_chat_node` assume `target_order` is already populated, so this raised a raw `KeyError` deep in the graph. Now calls `graph.aget_state(config)` first and returns a `400` with a clear "session not found or expired" message when `target_order` isn't set on that thread, before ever invoking the graph. `route_order` also switched from `state["target_order"]` to `state.get("target_order")` as defense-in-depth — TypedDict fields have no runtime default, so a key that's never been written still raises `KeyError` on direct indexing.
    - Note for CSA-11 (order-inquiry UI): this backend guard is a boundary check, not a substitute for the UI contract — the UI must never advance to the order-detail/conversation screen or call `/api/support/order/continue` when `/api/support/order/init` returns `target_order: null`; it should surface the `response` text and route the customer back to order selection instead.
    - `tests/test_customer_support.py::test_order_init_fail`: a `result["messages"][0].role == ROLE_AGENT` line was missing the `assert` keyword, so the check was evaluated and silently discarded. Fixed.
    - `services/demo_data.py::adjust_days_demo_data_testable`: indexed `customer_order_status_map[email]` positionally against `demo_customers[email]["latest_orders"]` with no bounds/existence check — the file's own `#TODO: Modify to have users with multiple orders` invites exactly the change (adding an order without a matching status entry) that would silently `IndexError`/`TypeError` at startup. Now raises a clear `ValueError` up front if an email is missing from the map or the two lists' lengths don't match.
    - `models/model.py::MAX_REASON_CHARS` had been cut from 500 to 50 with no explanation, which would reject any realistic free-text reason a human CSR types when approving/rejecting/flagging a refund via Slack once that flow is wired up. Reverted to 500.
    - `services/customer_support_service.py::invoke_order_continue_workflow`: removed a leftover debug `print(...)` statement.

- CSA-11: [UI] `order_inquiry` init and continue screens — no backend changes needed, everything CSA-10 (PR #8) shipped was already sufficient. Built as a single client component (`frontend/app/customer/order-inquiry/page.tsx`) with internal two-step state (`select` → `chat`) rather than two separate Next.js routes, matching this app's existing convention of one client component per screen with no shared routing/state between pages.
  - **Select step**: `session.customer_context.latest_orders` sorted descending by `order_date` client-side — the backend does not guarantee order (`models/model.py`'s own comment on `CustomerContext.latest_orders` explicitly punts "descending order" to the UI). Each order renders as a radio card (order id/date/total + itemized product × qty); "Continue" calls `POST /api/support/order/init`, reusing the existing `threadId` if the customer already initialized a different order this visit (see "order switching" below). A `target_order: null` response (failed lookup) stays on this screen and surfaces `response` as an error instead of advancing — this is the literal contract the ticket states ("DO NOT GO TO the order detail and conversation screen if the target_order is None"), and it's enforced here in the UI, not just by the backend's defensive `400` guard added in the CSA-10 fixes above.
  - **Chat step**: structured order-detail `<table>` (status badge, order/ship/estimated/delivered dates, tracking number, notes, total) plus an itemized `<table>`, chat transcript seeded with `/order/init`'s deterministic first reply, input box calling `POST /api/support/order/continue` on each round.
  - **Order switching within one visit**: since a return/refund can only process one order at a time but a customer may have more than one recent order, "Back to order selection" lets them restart the flow for a different order — it calls `/order/init` again but reuses the *same* `thread_id` rather than minting a new one (the endpoint already fully resets the order-specific state fields on every call), so the running `summary` naturally spans "customer looked at order A, then asked about order B" within one visit. Verified this actually happens by checking `data/checkpoints.sqlite` directly during manual testing: three separate `/order/init` calls (order A → order B → order A again) all landed under one `thread_id`. This button is disabled (grayed out, with a tooltip) when the customer has only one recent order — `sonya_ling1947@yahoo.com` in the demo data is the one seeded customer with two orders, kept specifically to exercise this path.
  - **`intent_for_return_refund` badge**: when `/order/continue` sets this flag, the chat screen shows a small "Return requested" badge next to that reply. This is intentionally temporary scaffolding (commented as such in the code) — there's no itemized-return screen yet (`api/support/return/start` from the `CLAUDE.md` architecture notes is a later ticket), so for now the agent's own natural-language `response` text is what actually tells the customer what happens next; the badge is just a visual acknowledgment to tear down once the real return/refund UI exists.
  - **Exit wiring**: `POST /api/support/exit` (summarize-on-exit, added in CSA-10) is now called — best-effort, swallowing failures, never blocking navigation — whenever the customer leaves an initialized order thread: pressing Exit (which also logs out, since Exit *is* the app's only logout action — there's no separate logout button anywhere), and also when navigating sideways to the general-inquiry screen via "Ask a general question" (present on both the select step and, per user feedback after initial review, also the chat step — a single-order customer's only options on the chat screen were otherwise "Back to order selection" (disabled) and "Exit" (full logout), with no lighter-weight way to switch to general inquiry). Retrofitted the same exit-call into `others/page.tsx`'s Exit button too, since it predates the `/api/support/exit` endpoint and had never been wired up.
  - **Known gaps, left as-is deliberately**: (1) a single-order customer's only way to leave the chat screen besides "Ask a general question" is still a full-logout Exit — consistent with every other screen in the app already using Exit as the sole logout mechanism, not a regression specific to this page; (2) the `exit`-then-`logout`-then-navigate helper is duplicated between `order-inquiry/page.tsx` and `others/page.tsx` rather than extracted into a shared hook — matches this codebase's established duplication-over-abstraction convention (no `components/` directory exists anywhere in `frontend/`), a "rule of three" candidate once a third page needs the same logic; (3) dates render in the viewer's local browser timezone (`Intl.DateTimeFormat` with no `timeZone` override) rather than a fixed reference zone — judged to be the more correct default for a customer-facing display, not a bug.
  - `frontend/lib/auth-context.tsx`'s `OrderItem` TypeScript interface had drifted from the backend (`non_refundable: boolean` vs. the actual `intimate_item: bool` field) since CSA-5/6/7 — fixed as part of this ticket since the new order-detail table is the first thing to actually render `OrderItem[]`.

- CSA-12: [API & workflow] `order_init` revisit detection — the problem: `order_init_chat_node` always showed the same static per-status template on `/order/init` regardless of whether the customer had already discussed this exact order (a lost-shipment report, say) in a prior turn or a prior visit. Re-asking "Is everything O.K.?" after already knowing the answer made the CSA look like it forgot the conversation.
  - **Node split**: `order_init_chat_node` became three nodes. `retrieve_target_order_node` — pure lookup, sets `target_order`, still handles the "order not found" `SYSTEM_ERROR_MESSAGE` case exactly as before. `detect_order_revisit_node` — new; runs only once an order is found; asks an LLM (`OrderRevisitEval{is_revisit: bool}`) whether order #X specifically has already been discussed, given `state["summary"]` (this thread) and long-term memory (injected as a `SystemMessage` by `invoke_order_init_workflow` on every `/order/init` call, not just the first — this is what makes cross-*session* revisits, not just same-thread ones, detectable). `order_init_static_response_node` — the old per-status template, unchanged in content, now with no retrieval logic in it, reachable only on a fresh inquiry.
  - **Routing on a revisit skips straight to the existing `order_continue_chat_node`** — no new node, no new prompt written. That node already reads `state["summary"]` and already covers lost-shipment/damaged/cancel scenarios in its system prompt, so it just naturally produces a context-aware reply instead of the canned line. Graph: `retrieve_target_order_node` → (not found → `summarize_node`; found → `detect_order_revisit_node`) → (fresh → `order_init_static_response_node` → `summarize_node`; revisit → `order_continue_chat_node`, its existing conditional routing unchanged).
  - **Debugging note, worth remembering**: first live test (real LLM, not mock) after Exit-and-re-login still showed the static template. Traced it end to end via direct `data/checkpoints.sqlite`/`data/stores.sqlite` inspection (not just the HTTP response) — confirmed the mechanism itself works: an isolated replay of the exact same customer/order/memory against the real LLM returned `is_revisit: True` with a genuine context-aware reply every time. Tried to pin the one observed miss on either `LLM_TEMPERATURE` or the prompt lumping memory into the raw conversation transcript — a controlled test crossing old/new prompt × old/new temperature (5 trials each) scored 5/5 correct on *all four* combinations, including the exact original setup. Root cause stayed inconclusive; treated as a rare LLM sampling fluke rather than a code defect, since it could not be reproduced under controlled conditions.
  - **Kept anyway, on their own merits**: `order_llm_for_revisit_eval` now gets its own `temperature=0.0` (classification benefits from determinism; the conversational nodes still want some variety, so this is scoped to just this one LLM instance). Long-term memory is now pulled into its own clearly labeled prompt section ("Here is what we remember about this customer from past visits") instead of being folded into the raw conversation transcript — applied to both `detect_order_revisit_node` and `order_continue_chat_node` (the latter needed it too: a revisit reaches it *before* `summarize_node` has pruned anything, so the injected memory `SystemMessage` is still sitting in `state["messages"]` at that point). Both nodes now `logger.info` their full prompt and the LLM's structured result, so a future debugging pass doesn't need a one-off script to see what actually went in and came out.
  - Also cleaned up, per user request: `general_faq_eval_node` had a `state["messages"][-2]`-based fallback reintroduced into its prompt — the exact fixed-index anti-pattern CSA-8's notes above already documented avoiding (memory is reliably available via the injected `SystemMessage`/`summary` already, no need to guess at a message-list position). Reverted to the simple `state.get("summary") or "None yet"` form.
  - Also fixed, per user's own edit: `order_continue_chat_node`'s escalate branch built a reason-specific header-prefixed message for the internal `ChatMessage`, but returned the bare `ESCALATE_MESSAGE` (no header) as the `"response"` value — meaning the API/UI never actually surfaced the header text to the customer, only the internal message log had it. Now returns the header-prefixed text on both.
  - Tests: `retrieve_target_order_node`/`order_init_static_response_node` cover what the old combined-node tests did; new tests for `detect_order_revisit_node` (both a faked-`True` case via a stub LLM, and the real mock-always-`None`-→-`False` case) and both new routing functions. 33/33 backend tests pass.

### Current API Endpoints
- `GET /api/health` — liveness check
- `POST /api/approval/start` — draft content for a task, pauses for human review (HITL interrupt)
- `POST /api/approval/decide` — resume the approval workflow with `approve` / `edit` / `reject`
- `POST/api/auth` - authorize role: `csr` (customer-support representative) or `customer`.  Users of different role access different screen
- `POST /api/support/general` — general/others FAQ inquiry (fuzzy/LLM match, escalate on low confidence); optional `thread_id` in the request/response resumes the same LangGraph thread across rounds
- `POST /api/support/order/init` - Initialize order inquiry by passing in `order_number_provided` from UI and the backend retrieve the `target_order` and start the `order_inquery` conversation
- `POST /api/support/order/continue` - UI calls this endpoint after the customer starts his/ her `order_inquery` and the endpoint can be called multiple time for each new input from the customer
- `POST /api/support/exit` - UI calls this end when the customer press `Exit` button 
- `GET /{full_path:path}` — catch-all serving the Next.js static export (`frontend/out`); see [Routing guard](#routing-guard)
