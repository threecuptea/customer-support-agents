services:
  custom-support:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # CHECKPOINT_DB/STORE_DB from .env resolve (via main.py's DATA_DIR) to
      # /app/data/<filename> — mounting it here is what makes that durable.
      - ./data:/app/data
    restart: unless-stopped
